{
  "name": "revierai-beta",
  "version": "0.1.0",
  "private": true,
  "description": "RevierAI iOS/Android Beta",
  "type": "module",
  "scripts": {
    "sync": "npx cap sync",
    "add:ios": "npx cap add ios",
    "add:android": "npx cap add android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}